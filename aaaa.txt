
例えば既にある。

GET /api/profiles/{hrid}

でも、編集画面に来た時点で hrid が分からない。

だから認証情報から取得するAPIを作る。
だから認証情報から取得するAPIを作る。

GET /api/profiles/me
@GetMapping("/me")
public ProfileResponse getMyProfile(
        Authentication authentication
) {
    String hrid = authentication.getName();

    return profileService.getProfile(hrid);
}

フロント
フロント

const [myProfile, setMyProfile] = useState<Profile>();

useEffect(() => {
    api.get("/profiles/me")
        .then(res => setMyProfile(res.data));
}, []);

以降

myProfile?.hrid

で使える。


修正版 NeedCard.tsx 完全版

これをそのままコピペで。

import {
  Card,
  CardContent,
  Box,
  Typography,
} from "@mui/material";
import ChatBubbleOutlineIcon from "@mui/icons-material/ChatBubbleOutline";
import PanToolAltIcon from "@mui/icons-material/PanToolAlt";

import type { Need } from "../models/Need";

type Props = {
  need: Need;
};

export default function NeedCard({ need }: Props): JSX.Element {
  return (
    <Card
      elevation={0}
      sx={{
        width: "100%",
        height: "100%",
        display: "flex",
        flexDirection: "column",
        justifyContent: "space-between",
        borderRadius: 4,
        border: "1px solid #e0e0e0",
        backgroundColor: "#fff",
        transition: "all 0.25s ease",
        cursor: "pointer",
        "&:hover": {
          transform: "translateY(-2px)",
          boxShadow: "0 6px 16px rgba(0,0,0,0.08)",
        },
      }}
    >
      <CardContent
        sx={{
          display: "flex",
          flexDirection: "column",
          height: "100%",
          p: 2,
          "&:last-child": {
            pb: 2,
          },
        }}
      >
        <Box sx={{ textAlign: "center", mb: 1 }}>
          <Typography
            sx={{
              display: "inline-block",
              px: 1.2,
              py: 0.4,
              borderRadius: 1,
              backgroundColor: "#114C88",
              color: "white",
              fontSize: 11,
              fontWeight: 700,
            }}
          >
            {need.categoryName}
          </Typography>
        </Box>

        <Typography
          sx={{
            fontSize: 18,
            fontWeight: 800,
            color: "black",
            textAlign: "center",
            lineHeight: 1.35,
            mb: 1.5,
            minHeight: 48,
          }}
        >
          {need.needTitle}
        </Typography>

        <Typography
          variant="body2"
          sx={{
            color: "text.secondary",
            fontSize: 13,
            lineHeight: 1.5,
            minHeight: 38,
            overflow: "hidden",
            display: "-webkit-box",
            WebkitLineClamp: 2,
            WebkitBoxOrient: "vertical",
            mb: 1.5,
          }}
        >
          {need.needDetail}
        </Typography>

        <Box
          sx={{
            mt: "auto",
            display: "flex",
            justifyContent: "space-between",
            alignItems: "end",
            gap: 1,
          }}
        >
          <Box>
            <Typography
              sx={{
                fontSize: 12,
                fontWeight: 700,
                color: "black",
              }}
            >
              {need.affiliatedName} {need.userName}
            </Typography>

            <Typography
              sx={{
                fontSize: 11,
                color: "text.secondary",
              }}
            >
              {need.createdTime?.slice(0, 10)}
            </Typography>
          </Box>

          <Box
            sx={{
              display: "flex",
              alignItems: "center",
              gap: 0.8,
              color: "black",
              fontSize: 13,
              fontWeight: 700,
            }}
          >
            <ChatBubbleOutlineIcon sx={{ fontSize: 17 }} />
            <span>{need.answerCount}</span>

            <PanToolAltIcon sx={{ fontSize: 17, color: "#FFD700" }} />
            <span>{need.empathyCount}</span>
          </Box>
        </Box>
      </CardContent>
    </Card>
  );
}
もし @mui/icons-material が入ってないと言われたら

その場合はインストール。

npm install @mui/icons-material

ただ、MUI使ってるなら普通は入ってる可能性高い。

今回のエラーの対応まとめ
修正前
import { Need } from "../models/Need";
import BsChatDots from "react-icons/bs";
import FaHandPaper from "react-icons/fa";
修正後
import ChatBubbleOutlineIcon from "@mui/icons-material/ChatBubbleOutline";
import PanToolAltIcon from "@mui/icons-material/PanToolAlt";

import type { Need } from "../models/Need";
